# Word Rush Word Data

The CSV files in this folder are loaded by level when the user changes the level selector.

Each CSV uses this shape:

```csv
english,japanese
apple,りんご
```

The current files are generated by `../scripts/generate-word-data.mjs` from the CC0 English-Japanese dictionary project EJDict.

Source: https://github.com/kujirahand/EJDict
